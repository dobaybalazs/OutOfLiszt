class ListItem {
  constructor(id, name, img, unit, price) {
    this.id = id;
    this.name = name;
    this.img = img;
    this.unit = unit;
    this.price = price;
  }
}

export default ListItem;
