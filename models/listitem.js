class ListItem {
  constructor(id, name, img, unit, price, categoryId) {
    this.id = id;
    this.name = name;
    this.img = img;
    this.unit = unit;
    this.price = price;
    this.categoryId = categoryId;
  }
}

export default ListItem;
